#include "encoder.h"

EncodeResult* Encoder::Init(EncoderConfig& InConfig)
{
	unimplemented();
	return new EncodeResult();
}

EncodeResult* Encoder::Encode()
{
	unimplemented();
	return new EncodeResult();
}